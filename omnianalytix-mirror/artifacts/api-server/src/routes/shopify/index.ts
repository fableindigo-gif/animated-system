import { Router } from "express";

// Shopify routes are reserved for future integrations. The previous
// `POST /api/shopify/discount` endpoint had no frontend caller and was
// removed during the 2026-04-23 polish pass.
const router = Router();

export default router;
