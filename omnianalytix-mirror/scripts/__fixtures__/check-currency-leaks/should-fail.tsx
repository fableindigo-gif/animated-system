// Each non-comment, non-blank line below is a known leak. The checker
// MUST flag every one of them. Lines that begin with `//` are skipped.
const a = `$${spend.toFixed(0)}`;
const b = `$${(n / 1_000).toFixed(1)}K`;
const c = `vs $${target}K target`;
const d = `Synced ${rows} · $${spend} spend`;
const e = `$${data.targets.cplCap}`;
